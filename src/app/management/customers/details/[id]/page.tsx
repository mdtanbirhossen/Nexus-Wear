import CustomerDetails from '@/components/customers/CustomerDetails';
import React from 'react';

const page = () => {
    return (
        <div className="p-6">
            <CustomerDetails />
        </div>
    );
};

export default page;
